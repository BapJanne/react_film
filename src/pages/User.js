import React from 'react';
import Navigation from '../components/Navigation';

const User = () => {
    return (
        <div>
            <Navigation/>
            <h2>User</h2>
        </div>
    );
};

export default User;